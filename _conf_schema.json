{
  "emotion_map": {
    "description": "表情映射",
    "type": "object",
    "invisible": true,
    "items": {
      "生气": { "type": "string", "default": "angry" },
      "开心": { "type": "string", "default": "happy" },
      "悲伤": { "type": "string", "default": "sad" },
      "惊讶": { "type": "string", "default": "surprised" },
      "疑惑": { "type": "string", "default": "confused" },
      "色色": { "type": "string", "default": "color" },
      "色": { "type": "string", "default": "color" },
      "死机": { "type": "string", "default": "cpu" },
      "笨蛋": { "type": "string", "default": "fool" },
      "给钱": { "type": "string", "default": "givemoney" },
      "喜欢": { "type": "string", "default": "like" },
      "看": { "type": "string", "default": "see" },
      "害羞": { "type": "string", "default": "shy" },
      "下班": { "type": "string", "default": "work" },
      "剪刀": { "type": "string", "default": "scissors" },
      "不回我": { "type": "string", "default": "reply" },
      "喵": { "type": "string", "default": "meow" },
      "八嘎": { "type": "string", "default": "baka" },
      "早": { "type": "string", "default": "morning" },
      "睡觉": { "type": "string", "default": "sleep" },
      "唉": { "type": "string", "default": "sigh" }
    },
    "default": {}
  },
  "memes_path": {
    "description": "表情包存储路径",
    "type": "string",
    "hint": "请输入表情包存储路径",
    "default": "memes",
    "invisible": true
  },
  "image_host": {
    "description": "选择的图床",
    "type": "string",
    "hint": "请选择要使用的图床服务",
    "default": "stardots",
    "options": ["stardots"]
  },
  "image_host_config": {
    "description": "图床配置",
    "type": "object",
    "items": {
      "stardots": {
        "description": "Stardots 图床配置",
        "type": "object",
        "items": {
          "key": {
            "description": "API Key",
            "type": "string",
            "hint": "请输入 Stardots 的 API Key"
          },
          "secret": {
            "description": "API Secret",
            "type": "string",
            "hint": "请输入 Stardots 的 API Secret"
          },
          "space": {
            "description": "空间名称",
            "type": "string",
            "hint": "请输入 Stardots 的空间名称",
            "default": "1350989414"
          }
        }
      }
    }
  },
  "webui_port": {
    "description": "Web UI 端口号",
    "type": "int",
    "default": 5000
  }
}
